# scripts/

Build, bundle, and release helpers. Populated as milestones are reached.

| Script | Added at | Purpose |
|---|---|---|
| `refresh-fixtures.sh` | M2 | Re-record scraper HTML fixtures |
| `bundle-windows.sh` | M5 | Package Windows installer |
| `bundle-linux.sh` | M5 | Package AppImage + deb |
| `bundle-android.sh` | M5 | Package per-ABI APKs |
