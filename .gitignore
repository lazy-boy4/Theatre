# Rust
target/
# Lockfiles ARE committed (tech-stack.md §1: committed + authoritative).
# Cargo.lock
*.pdb

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
app/.dart_tool/
# app/pubspec.lock — committed, see above.

# IDE
.idea/
.vs/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Generated FFI bindings (committed after codegen)
# app/lib/core/bridge/ — keep committed

# Vendor checksums (MANIFEST committed; binaries NOT)
vendor/ffmpeg/bin/
vendor/yt-dlp/bin/

# Secrets
.env
*.key
secrets.json
