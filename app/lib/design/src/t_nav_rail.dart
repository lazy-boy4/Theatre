import 'package:flutter/material.dart';

// TNavRail — Theatre token-layer widget stub.
// TODO(T0.4): implement with M3E tokens.
class TNavRail extends StatelessWidget {
  const TNavRail({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
