import 'package:flutter/material.dart';

// TNavDrawer — Theatre token-layer widget stub.
// TODO(T0.4): implement with M3E tokens.
class TNavDrawer extends StatelessWidget {
  const TNavDrawer({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
